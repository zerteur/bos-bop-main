# Copier ce fichier vers .env puis adapter les valeurs.

# Base de données SQLite (chemin relatif au dossier prisma/)
DATABASE_URL="file:./bosbop.db"

# Secret de session pour l'administration (générer une longue chaîne aléatoire :
#   openssl rand -hex 32
SESSION_SECRET="changez-moi-avec-une-longue-chaine-aleatoire"

# URL publique du site (utilisée pour les liens de partage)
SITE_URL="https://www.bos-bop.fr"

# Identifiants du premier compte administrateur (utilisés par `npm run db:seed`)
ADMIN_EMAIL="admin@bos-bop.fr"
ADMIN_PASSWORD="admin"

# Cookie de session administrateur : marqué "Secure" automatiquement si la
# requête arrive en HTTPS (via l'en-tête x-forwarded-proto d'un reverse
# proxy). Ne définir cette variable QUE si l'auto-détection ne convient pas
# à votre hébergement : "1" force Secure (site en HTTPS sans proxy devant),
# "0" le désactive (dépannage local en HTTP). Laisser vide dans les autres cas.
# COOKIE_SECURE=""

# Paiement en ligne (boutique /livres) : se configure normalement depuis le
# tableau de bord (Réglages -> Paiement en ligne), voir le README section
# « Connecter Stripe ». Ces deux variables sont une alternative réservée aux
# déploiements entièrement pilotés par fichiers de configuration : si
# définies, elles sont prioritaires sur les valeurs enregistrées depuis le
# tableau de bord. Tant qu'aucune des deux options n'est configurée, la
# boutique fonctionne normalement sans paiement en ligne (règlement organisé
# manuellement avec chaque client).
# STRIPE_SECRET_KEY="sk_live_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."

# Anti-spam du formulaire de contact (reCAPTCHA v2) : se configure normalement
# depuis le tableau de bord (Réglages -> Protection anti-robot), voir le README
# section « Connecter reCAPTCHA ». Ces deux variables sont une alternative
# réservée aux déploiements pilotés par fichiers de configuration : si
# définies, elles sont prioritaires sur les valeurs enregistrées depuis le
# tableau de bord. Sans clé, le formulaire reste protégé par un champ piège et
# une limite de fréquence.
# RECAPTCHA_SITE_KEY="6L..."
# RECAPTCHA_SECRET_KEY="6L..."
